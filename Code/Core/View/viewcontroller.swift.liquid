//
//  {{ module_info.file_name }}
//  {{ module_info.project_name }}
//
//  Created by {{ developer.name }} on {{ date }}.
//  Copyright © {{ year }} {{ developer.company }}. All rights reserved.
//

import UIKit

protocol {{ module_info.name }}Protocol: AnyObject {
  func prepare(with viewModels: [{{ module_info.name }}ViewModel])
}

class {{ module_info.name }}ViewController: UICollectionViewController, {{ module_info.name }}Protocol {
  private struct Constants {
    static let storyboardName = "\({{ module_info.name }})"
    static let storyboarID = String(describing: {{ module_info.name }}ViewController.self)
  }

  static func buildViewController() -> {{ module_info.name }}ViewController! {
    let storyboard = UIStoryboard(name: Constants.storyboardName, bundle: .main)
    return storyboard.instantiateInitialViewController() as? {{ module_info.name }}ViewController
  }

  private var presenter: {{ module_info.name }}PresenterProtocol?

  override func viewDidLoad() {
    super.viewDidLoad()

    presenter = {{ module_info.name }}Presenter(with: self)
    presenter?.present()
  }

  func prepare(with viewModels: [{{ module_info.name }}ViewModel]) {
    // Display the viewModels on collectionview.
  }
}
